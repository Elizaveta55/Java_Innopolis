package BankSystem.Client;

public class VipClient extends Client {

    // constructor with initialization of super-class's fields
    // which invokes constructor of super-class
    public VipClient(String name, String birthday, String gender) {
        super(name, birthday, gender);
    }
}
